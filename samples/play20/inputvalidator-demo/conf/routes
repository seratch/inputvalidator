# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /login/input                controllers.Application.loginInput
POST    /login/submit               controllers.Application.loginSubmit
GET     /upload/input               controllers.Application.uploadInput
POST    /upload/submit              controllers.Application.uploadSubmit

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

